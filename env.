VITE_FIREBASE_API_KEY=AIzaSyDAQj08HDReplTZAnxabBJdtbD38_wdsYE

# Your real Discord invite, e.g. https://discord.gg/xxxxxxx — shown on the
# Support page. Leave empty and the button shows "Discord link not set".
VITE_DISCORD_INVITE=

# Server-only. Shared secret the obfuscation bot/engine sends as
# `Authorization: Bearer <value>` when POSTing finished jobs to /api/jobs.
# Never prefix with VITE_ or it will be bundled into client JS.
JOBS_INGEST_SECRET=

# Server-only, used by /api/health-check (see that file for details).
HEALTH_CHECK_SECRET=
